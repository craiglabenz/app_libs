name: auth_flutter
description: "A new Flutter package project."
version: 0.0.1
homepage: null
publish_to: none

environment:
  sdk: ^3.8.1
  flutter: ">=1.17.0"

dependencies:
  auth_client:
    path: ../auth_client
  client_auth:
    path: ../../../packages/client_auth
  data_layer: ^0.0.1-beta.8
  flutter:
    sdk: flutter
  flutter_secure_storage: ^10.0.0-beta.4
  logging: any
  serverpod_client: ^2.9.1
  shared_data:
    path: ../../../packages/shared_data
  shared_preferences: ^2.5.3

dev_dependencies:
  flutter_lints: ^5.0.0
  flutter_test:
    sdk: flutter
