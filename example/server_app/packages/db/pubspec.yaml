name: db
description: Database access layer
version: 0.1.0+1
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  auth:
    path: ../auth
  dartz: ^0.10.1
  drift: ^2.14.1
  drift_postgres:
    path: ../../../../../../simolus3/drift/extras/drift_postgres
  postgres: ^3.0.4
  shared_data:
    path: ../../../../packages/shared_data

dev_dependencies:
  build_runner: ^2.4.7
  drift_dev: ^2.14.1
  mocktail: ^1.0.0
  test: ^1.19.2
  very_good_analysis: ^5.1.0
