name: server_app
description: An new Dart Frog application
version: 1.0.0+1
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  auth:
    path: packages/auth
  dart_frog: ^1.0.0
  db:
    path: packages/db
  shared_data:
    path: ../../packages/shared_data

dev_dependencies:
  mocktail: ^1.0.0
  test: ^1.19.2
  very_good_analysis: ^5.1.0
